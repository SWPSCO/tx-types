[package]
name = "murmur3"
version = "0.5.2"
authors = ["Stu Small <stuart.alan.small@gmail.com>"]
description = "A rust implementation of Murmur3 hash"
repository = "https://github.com/stusmall/murmur3"
keywords = ["hash", "murmur3", "murmur"]
license = "MIT/Apache-2.0"
edition = "2021"

[dependencies]
[dev-dependencies]
# Optional test harness dependencies are intentionally disabled in this fork to
# avoid pulling in the native murmur3 bindings during firmware-oriented builds.
